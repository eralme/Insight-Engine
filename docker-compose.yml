version: '3.8'
services:
  insight-engine:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./chroma_db:/app/chroma_db  # Persist vector DB on host
      - ./.env:/app/.env            # Inject API Keys
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}